define( function() {
        //return an object to define the "my/shirt" module.
        return {
            color: "blue",
            size: "large",
            hello: function(text) {
                return "hello : " + text;
            }
        }
    }
);